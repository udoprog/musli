[package]
name = "musli-zerocopy"
version = "0.1.0"
edition = "2021"

[features]
default = ["std", "build"]
std = ["alloc"]
alloc = []
build = ["alloc"]

[dependencies]
rand = { version = "0.8.5", default-features = false, features = ["small_rng"] }

[dev-dependencies]
anyhow = "1.0.72"
